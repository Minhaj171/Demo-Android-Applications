<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.test_project">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_sphero"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_sphero"
        android:supportsRtl="true"
        android:theme="@style/Theme.Testproject">
        <activity android:name=".DashBoard"></activity>
        <activity android:name=".IndexPage" />
        <activity android:name=".LoginForm" />
        <activity android:name=".SignupForm" />
        <activity android:name=".SimpleCalculator" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>